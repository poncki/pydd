[tox]
envlist = py35

[testenv]
passenv = CI TRAVIS TRAVIS_* TOXENV

deps =
    check-manifest
    readme_renderer
    flake8
    pytest
    pylint
    codecov

commands =
    check-manifest --ignore tox.ini,tests*
    python setup.py check -m -r -s
    py.test tests/
    flake8 --exit-zero src/app tests/
    pylint src/pydd/ tests/
    codecov -e TOXENV

[flake8]
exclude = .tox,*.egg,build,.__stash__
select = E,W,F
